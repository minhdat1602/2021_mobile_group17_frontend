import 'package:flutter/material.dart';

class SizeGuideWidget extends StatelessWidget {
  SizeGuideWidget({Key key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
